//brute force
// class Solution {
// public:
//     vector<int> maxSlidingWindow(vector<int>& nums, int k) {
//         int n = nums.size();
//         vector<int> ans;
        
//         // 'i' represents the start of the current window
//         for (int i = 0; i <= n - k; i++) {
//             int maxi = nums[i];
            
//             // 'j' scans the rest of the elements in the window of size k
//             for (int j = i; j < i + k; j++) {
//                 maxi = max(maxi, nums[j]);
//             }
            
//             // Push the max element found in this window
//             ans.push_back(maxi);
//         }
        
//         return ans;
//     }
// };

//tc=o(n) because each elemennt deque mai  sirf ek hi paar push ho rha hai aur ek hi baar pop ho rha hai.(multiple time nhi ho rha thats why o(n))
//monotonic decreasing queue
class Solution {
public:
    vector<int> maxSlidingWindow(vector<int>& nums, int k) {
        int n = nums.size();
        vector<int> ans;
        deque<int> dq; // Will store indices of elements

        for (int i = 0; i < n; i++) {
            // 1. Remove indices that have slid out of the window
            if (!dq.empty() && dq.front() == i - k) {
                dq.pop_front();
            }

            // 2. Remove elements smaller than the current element from the back
            while (!dq.empty() && nums[dq.back()] < nums[i]) {
                dq.pop_back();
            }

            // Add the current element's index
            dq.push_back(i);

            // 3. Once the first window is complete, start adding to the answer
            if (i >= k - 1) {
                ans.push_back(nums[dq.front()]);
            }
        }
        
        return ans;
    }
};
